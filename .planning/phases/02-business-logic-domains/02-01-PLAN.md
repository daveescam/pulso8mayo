---
phase: 02-business-logic-domains
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
- lib/db/schema/workflows.ts
- lib/db/schema/index.ts
autonomous: true
requirements:
- DOM-03
must_haves:
  truths:
  - "lib/db/schema/workflows.ts exists with all workflow tables"
  - "All workflow-related enums exported from workflows.ts"
  - "lib/db/schema/index.ts exports workflows module"
  - "TypeScript compilation passes with no errors"
  - "All workflow tables reference core tables correctly (companies.id, branches.id)"
  artifacts:
  - path: "lib/db/schema/workflows.ts"
    provides: "Workflow tables and enums"
    min_lines: 200
  - path: "lib/db/schema/index.ts"
    provides: "Updated barrel exports including workflows module"
    exports: ["* from './workflows'"]
  key_links:
  - from: "workflows.ts"
    to: "core.ts"
    via: "import for foreign key references (companies, branches)"
    pattern: "import.*core"
---

<objective>
Extract workflow-related tables into a dedicated workflows module.

Purpose: Create the workflows domain module containing all workflow, scheduling, event, and incident management tables. This is a core business logic domain that manages task automation and compliance workflows.

Output:
- lib/db/schema/workflows.ts with workflow tables and enums
- Updated lib/db/schema/index.ts with workflows exports
- All cross-domain references (to companies, branches) properly handled
</objective>

<execution_context>
@C:/Users/david/pulso29/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/david/pulso29/lib/db/schema.ts
@C:/Users/david/pulso29/lib/db/schema/index.ts
@C:/Users/david/pulso29/lib/db/schema/core.ts
@C:/Users/david/pulso29/AGENTS.md

Current state: Phase 1 complete - auth.ts and core.ts modules exist, schema.ts re-exports from index.ts

Workflow tables to extract from schema.ts:
Tables:
- workflowTemplates (lines ~84-102) - workflow definitions
- workflowInstances (lines ~51-69) - running workflow instances
- workflowInstanceSteps (lines ~71-82) - steps within instances
- workflowSchedules (lines ~104-139) - scheduled workflows
- workflowAssignments (lines ~158-183) - task assignments
- eventTriggers (lines ~141-156) - event-based triggers
- incidents (lines ~185-208) - incident tracking

Enums:
- scheduleFrequencyEnum (line ~27) - DAILY, WEEKLY, MONTHLY, ONCE
- assignmentTypeEnum (line ~28) - ROLE, USER, AUTO, MANUAL
- assignmentStatusEnum (line ~29) - PENDING, NOTIFIED, STARTED, COMPLETED, OVERDUE
- priorityEnum (line ~30) - LOW, MEDIUM, HIGH, URGENT
- notificationTypeEnum (line ~31) - info, warning, error, success
- incidentSeverityEnum (line ~23) - CRITICAL, WARNING, FATAL
- incidentStatusEnum (line ~24) - DETECTED, IN_REMEDIATION, RESOLVED, ESCALATED

Cross-domain references:
- workflowTemplates.companyId → companies.id
- workflowTemplates.branchId → branches.id
- workflowInstances.branchId → branches.id
- workflowSchedules.branchId → branches.id
- eventTriggers.branchId → branches.id
- workflowAssignments.assignedTo → users.id (via text, FK implied)
- workflowAssignments.assignedBy → users.id (via text)
- incidents.branchId → branches.id

Drizzle ORM patterns:
- Uses pgTable, text, timestamp, boolean, uuid, jsonb, integer, pgEnum
- References other tables via foreign key columns (not inline references)
- Enums defined with pgEnum
</context>

<tasks>
<task type="auto">
<name>Task 1: Create workflows.ts module with tables and enums</name>
<files>lib/db/schema/workflows.ts</files>
<read_first>
- lib/db/schema.ts (source tables and enums)
- lib/db/schema/core.ts (for companies, branches references)
</read_first>
<action>Create lib/db/schema/workflows.ts with all workflow tables and enums:

1. Add imports:
import { pgTable, text, timestamp, boolean, uuid, jsonb, integer, pgEnum } from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";
import { companies, branches } from "./core";

2. Define enums (exact values from schema.ts):
export const scheduleFrequencyEnum = pgEnum("schedule_frequency", ['DAILY', 'WEEKLY', 'MONTHLY', 'ONCE']);
export const assignmentTypeEnum = pgEnum("assignment_type", ['ROLE', 'USER', 'AUTO', 'MANUAL']);
export const assignmentStatusEnum = pgEnum("assignment_status", ['PENDING', 'NOTIFIED', 'STARTED', 'COMPLETED', 'OVERDUE']);
export const priorityEnum = pgEnum("priority", ['LOW', 'MEDIUM', 'HIGH', 'URGENT']);
export const notificationTypeEnum = pgEnum("notification_type", ['info', 'warning', 'error', 'success']);
export const incidentSeverityEnum = pgEnum("incident_severity", ['CRITICAL', 'WARNING', 'FATAL']);
export const incidentStatusEnum = pgEnum("incident_status", ['DETECTED', 'IN_REMEDIATION', 'RESOLVED', 'ESCALATED']);

3. Define workflowTemplates table with all columns:
- id: uuid primaryKey default gen_random_uuid()
- companyId: uuid notNull (references companies.id)
- branchId: uuid (references branches.id, optional)
- name: text notNull
- description: text
- steps: jsonb notNull default sql`'[]'::jsonb`
- category: text default 'GENERAL'
- complianceType: text
- regulationSection: text
- requiredFrequency: text
- isCritical: boolean default false
- active: boolean default true
- createdAt: timestamp defaultNow()
- updatedAt: timestamp defaultNow()

4. Define workflowInstances table with all columns:
- id: uuid primaryKey
- workflowTemplateId: text notNull
- branchId: uuid notNull (FK to branches)
- assigneeId: text
- sessionId: uuid
- scheduleId: uuid
- assignmentId: uuid
- status: text default 'PENDING'
- startedAt: timestamp
- completedAt: timestamp
- dueDate: timestamp
- priority: priorityEnum default 'MEDIUM'
- currentStepId: text
- data: jsonb default sql`'{}'::jsonb`
- score: integer
- createdAt: timestamp defaultNow()
- updatedAt: timestamp defaultNow()

5. Define workflowInstanceSteps table:
- id: uuid primaryKey
- instanceId: uuid notNull (FK to workflowInstances)
- stepId: text notNull
- status: text default 'PENDING'
- value: jsonb
- aiAnalysis: jsonb
- evidenceUrl: text
- comment: text
- completedAt: timestamp
- completedBy: text

6. Define workflowSchedules table with all columns (lines ~104-139):
- id: uuid primaryKey
- templateId: text notNull
- branchId: uuid notNull (FK to branches)
- assignmentType: assignmentTypeEnum notNull default 'AUTO'
- assignedRole: roleEnum (import from auth or use text)
- assignedUserId: text
- frequency: scheduleFrequencyEnum notNull
- dayOfWeek: integer
- dayOfMonth: integer
- timeOfDay: text
- startDate: timestamp notNull
- endDate: timestamp
- isActive: boolean default true
- title: text notNull
- description: text
- priority: priorityEnum default 'MEDIUM'
- lastExecutedAt: timestamp
- nextExecutionAt: timestamp
- executionCount: integer default 0
- createdBy: text notNull
- createdAt: timestamp defaultNow()
- updatedAt: timestamp defaultNow()

7. Define eventTriggers table with all columns (lines ~141-156):
- id: uuid primaryKey
- templateId: text notNull
- branchId: uuid notNull (FK to branches)
- eventName: text notNull
- conditions: jsonb
- isActive: boolean default true
- createdBy: text notNull
- createdAt: timestamp defaultNow()
- updatedAt: timestamp defaultNow()

8. Define workflowAssignments table with all columns (lines ~158-183):
- id: uuid primaryKey
- instanceId: uuid notNull (FK to workflowInstances)
- scheduleId: uuid
- assignedTo: text notNull
- assignedBy: text
- assignmentType: assignmentTypeEnum notNull
- status: assignmentStatusEnum default 'PENDING'
- notifiedAt: timestamp
- startedAt: timestamp
- completedAt: timestamp
- dueDate: timestamp
- isOverdue: boolean default false
- priority: priorityEnum default 'MEDIUM'
- notes: text
- createdAt: timestamp defaultNow()
- updatedAt: timestamp defaultNow()

9. Define incidents table with all columns (lines ~185-208):
- id: uuid primaryKey
- instanceId: uuid notNull (FK to workflowInstances)
- stepId: text notNull
- branchId: uuid notNull (FK to branches)
- severity: incidentSeverityEnum notNull
- status: incidentStatusEnum default 'DETECTED'
- title: text notNull
- description: text
- detectedBy: text
- initialValue: jsonb
- targetValue: jsonb
- photoUrl: text
- metadata: jsonb
- remediationProtocol: jsonb
- escalationChain: jsonb
- currentAttempt: integer default 0
- maxAttempts: integer default 1
- resolution: text
- resolvedBy: text
- resolvedAt: timestamp
- createdAt: timestamp defaultNow()
- updatedAt: timestamp defaultNow()

Note: assignedRole uses roleEnum which is in auth.ts. Options:
- Import roleEnum from './auth' (adds auth dependency)
- Use text type instead (simpler, no new dependency)
Recommendation: Import roleEnum from auth since workflows module may need user role checks.</action>
<verify>
<automated>findstr /c:"export const workflowTemplates" lib\db\schema\workflows.ts && findstr /c:"export const workflowInstances" lib\db\schema\workflows.ts && findstr /c:"export const scheduleFrequencyEnum" lib\db\schema\workflows.ts</automated>
</verify>
<done>workflows.ts created with all workflow tables and enums, imports from core.ts for foreign key references</done>
</task>

<task type="auto">
<name>Task 2: Update index.ts with workflows exports</name>
<files>lib/db/schema/index.ts</files>
<read_first>
- lib/db/schema/index.ts (current state)
- lib/db/schema/workflows.ts (to verify exports)
</read_first>
<action>Update lib/db/schema/index.ts to include workflows module:

1. Read current index.ts - should have:
   export * from './auth';
   export * from './core';

2. Add workflows export after core:
   export * from './auth';
   export * from './core';
   export * from './workflows';

3. Maintain ordering by dependency:
   - auth first (most foundational)
   - core second (depends on auth for users FK)
   - workflows third (depends on core for companies/branches)

4. Verify no duplicate exports between modules

5. Ensure proper newlines at end of file</action>
<verify>
<automated>findstr /c:"export \* from './workflows'" lib\db\schema\index.ts</automated>
</verify>
<done>index.ts updated with workflows module export in correct dependency order</done>
</task>

<task type="auto">
<name>Task 3: Verify TypeScript compilation</name>
<files>lib/db/schema/workflows.ts, lib/db/schema/index.ts</files>
<action>Verify workflows module compiles correctly:

1. Run TypeScript compiler:
   pnpm tsc --noEmit

2. Check for errors:
   - Cannot find module './core' (should not happen, core.ts exists)
   - Cannot find module './auth' (if importing roleEnum)
   - Duplicate identifier errors
   - Type mismatch in foreign key references

3. If errors with roleEnum:
   - Option A: Add import { roleEnum } from "./auth"
   - Option B: Change assignedRole to text type

4. Verify all enum exports are accessible

5. Confirm no circular dependencies introduced</action>
<verify>
<automated>pnpm tsc --noEmit 2>&1 | findstr /c:"error TS" | find /c /v ""</automated>
</verify>
<done>TypeScript compilation passes with zero errors, all workflow tables properly typed</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| workflows → core | Import dependency for companies/branches FKs |
| workflows → auth | Optional import for roleEnum |
| index.ts → workflows | Barrel export boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Denial of Service | import failure | mitigate | Verify core.ts exists before import |
| T-02-01-02 | Information Disclosure | roleEnum exposure | accept | roleEnum is already public in auth.ts |
</threat_model>

<verification>
- [ ] lib/db/schema/workflows.ts created with 7 tables
- [ ] All workflow enums exported (7 enums)
- [ ] index.ts exports workflows module
- [ ] TypeScript compilation passes
- [ ] No circular dependencies
- [ ] Foreign key references compile correctly
</verification>

<success_criteria>
Measurable:
- 7 tables migrated (workflowTemplates, workflowInstances, workflowInstanceSteps, workflowSchedules, workflowAssignments, eventTriggers, incidents)
- 7 enums migrated (scheduleFrequency, assignmentType, assignmentStatus, priority, notificationType, incidentSeverity, incidentStatus)
- TypeScript compilation: 0 errors
- Cross-domain references: companies, branches FKs work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-domains/02-01-SUMMARY.md`
</output>
