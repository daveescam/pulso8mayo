---
phase: 02-business-logic-domains
plan: 03
type: execute
wave: 2
depends_on:
- 02-01
- 02-02
files_modified:
- lib/db/schema/employees.ts
- lib/db/schema/index.ts
autonomous: true
requirements:
- DOM-05
must_haves:
  truths:
  - "lib/db/schema/employees.ts exists with all employee tables"
  - "All employee-related enums exported from employees.ts"
  - "lib/db/schema/index.ts exports employees module"
  - "TypeScript compilation passes with no errors"
  - "No circular dependencies with labor.ts (vacationRequests shared handling)"
  artifacts:
  - path: "lib/db/schema/employees.ts"
    provides: "Employee tables (profiles, contracts, documents, onboarding, benefits, performance, leaves)"
    min_lines: 600
  - path: "lib/db/schema/index.ts"
    provides: "Updated barrel exports including employees module"
    exports: ["* from './employees'"]
  key_links:
  - from: "employees.ts"
    to: "core.ts"
    via: "import for companies, branches FKs"
    pattern: "import.*core"
  - from: "employees.ts"
    to: "auth.ts"
    via: "import for users FKs"
    pattern: "import.*auth"
---

<objective>
Extract employee-related tables into a dedicated employees module.

Purpose: Create the employees domain module containing all HR, payroll, benefits, performance, and document management tables. This is the largest domain with ~25+ tables covering employee lifecycle from onboarding to offboarding.

Output:
- lib/db/schema/employees.ts with employee tables and enums
- Updated lib/db/schema/index.ts with employees exports
- All cross-domain references properly handled
- vacationRequests table ownership resolved (may need to stay in one module)
</objective>

<execution_context>
@C:/Users/david/pulso29/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/david/pulso29/lib/db/schema.ts
@C:/Users/david/pulso29/lib/db/schema/index.ts
@C:/Users/david/pulso29/lib/db/schema/core.ts
@C:/Users/david/pulso29/lib/db/schema/auth.ts

Employee tables to extract from schema.ts (many tables, grouped by category):

Profile & Documents:
- employeeProfiles (lines ~1023-1090) - extends users table with HR data
- employeeDocuments (lines ~352-395) - document management

Contracts & Compensation:
- employeeContracts (lines ~1095-1153) - contract details
- salaryHistory (lines ~1156-1181) - salary changes

Audit & Lifecycle:
- employeeAuditLogs (lines ~1184-1215) - audit trail
- employeeOnboarding (lines ~1218-1248) - onboarding tracking
- onboardingSteps (lines ~1251-1277) - onboarding checklist
- employeeOffboarding (lines ~1280-1340) - offboarding process

Benefits & Training:
- employeeBenefits (lines ~1343-1377) - benefits tracking
- employeeTraining (lines ~1380-1422) - training records
- vacationAccruals (lines ~1425-1457) - vacation accrual

Performance:
- performanceReviews (lines ~1477-1510)
- performanceReviewCriteria (lines ~1513-1529)
- performanceReviewResponses (lines ~1532-1543)
- performanceGoals (lines ~1546-1570)

Leave Management:
- leaveTypes (lines ~1573-1591)
- leaveRequests (lines ~1594-1626)
- leaveBalances (lines ~1629-1643)

Note: vacationRequests (lines ~924-954) appears to be a duplicate/similar table to leaveRequests.
Check if vacationRequests should be merged with leaveRequests or kept separate.

Communications (might belong in communications module):
- employeeCommunications (lines ~1646-1677)
- communicationReadReceipts (lines ~1680-1685)
- messageTemplates (lines ~1688-1708)

Enums:
- documentTypeEnum (lines ~353-356)
- documentStatusEnum (line ~357)
- employeeStatusEnum (lines ~961-963)
- contractTypeEnum (lines ~967-968)
- workRegimeEnum (lines ~971-972)
- paymentMethodEnum (lines ~976-977)
- genderEnum (lines ~981-982)
- maritalStatusEnum (lines ~986-988)
- bloodTypeEnum (lines ~991-993)
- auditActionEnum (lines ~996-998)
- onboardingStepStatusEnum (lines ~1001-1003)
- offboardingReasonEnum (lines ~1006-1009)
- reviewTypeEnum (line ~1012)
- reviewStatusEnum (line ~1013)
- goalStatusEnum (line ~1014)
- leaveTypeEnum (lines ~1017-1019)
- leaveStatusEnum (line ~1020)
- vacationStatusEnum (lines ~922-923)
- performanceReviewTypeEnum (lines ~1468-1469)
- performanceReviewStatusEnum (lines ~1469-1470)
- performanceGoalStatusEnum (line ~1470)
- performanceCategoryEnum (lines ~1471-1472)
- leaveRequestStatusEnum (line ~1474)

Cross-domain references:
- Most tables have userId → users.id (text)
- Most tables have companyId → companies.id
- Most tables have branchId → branches.id (optional)
- employeeContracts references employeeDocuments.id
- salaryHistory references employeeContracts.id, employeeDocuments.id
- onboardingSteps references employeeOnboarding.id, employeeDocuments.id
- employeeOffboarding references users.id, companies.id, branches.id
- employeeBenefits references employeeDocuments.id
- employeeTraining references employeeDocuments.id
- vacationAccruals references users.id, companies.id
- performanceReviews references users.id (x2), companies.id, branches.id
- performanceReviewResponses references performanceReviews.id, performanceReviewCriteria.id
- performanceGoals references users.id, companies.id, branches.id
- leaveRequests references users.id, companies.id, branches.id, leaveTypes.id, employeeDocuments.id
- leaveBalances references users.id, leaveTypes.id
- employeeCommunications references companies.id, branches.id
- communicationReadReceipts references employeeCommunications.id, users.id
- messageTemplates references companies.id
</context>

<tasks>
<task type="auto">
<name>Task 1: Create employees.ts module structure</name>
<files>lib/db/schema/employees.ts</files>
<read_first>
- lib/db/schema.ts (source tables and enums)
- lib/db/schema/core.ts
- lib/db/schema/auth.ts
</read_first>
<action>Create lib/db/schema/employees.ts with all employee tables and enums.

This is a large file (~600+ lines expected). Structure it in sections:

1. Imports:
import { pgTable, text, timestamp, boolean, uuid, jsonb, integer, pgEnum } from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";
import { companies, branches } from "./core";
import { users } from "./auth";

2. Section A: Document Enums (lines ~352-357 area)
export const documentTypeEnum = pgEnum("document_type", [
  'CONTRACT', 'ID', 'PROOF_OF_ADDRESS', 'TAX_ID', 'BANK_INFO',
  'CERTIFICATE', 'TRAINING', 'MEDICAL_EXAM', 'PERMIT', 'OTHER'
]);
export const documentStatusEnum = pgEnum("document_status", ['PENDING', 'VALIDATED', 'EXPIRED', 'REJECTED']);

3. Section B: Profile Enums (lines ~961-993 area)
export const employeeStatusEnum = pgEnum("employee_status", [
  'ONBOARDING', 'ACTIVE', 'ON_LEAVE', 'SUSPENDED', 'TERMINATED', 'RESIGNED'
]);
export const contractTypeEnum = pgEnum("contract_type", [
  'DETERMINATE', 'INDETERMINATE', 'PROBATION', 'TRAINING', 'SEASONAL', 'PART_TIME'
]);
export const workRegimeEnum = pgEnum("work_regime", [
  'DAILY', 'MIXED', 'NIGHT', 'SPLIT_SHIFT', 'ON_CALL'
]);
export const paymentMethodEnum = pgEnum("payment_method", [
  'BANK_TRANSFER', 'CHECK', 'CASH', 'PAYROLL_CARD'
]);
export const genderEnum = pgEnum("gender", ['MALE', 'FEMALE', 'OTHER', 'PREFER_NOT_TO_SAY']);
export const maritalStatusEnum = pgEnum("marital_status", [
  'SINGLE', 'MARRIED', 'DIVORCED', 'WIDOWED', 'COMMON_LAW'
]);
export const bloodTypeEnum = pgEnum("blood_type", [
  'A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-'
]);

4. Section C: Audit & Lifecycle Enums
export const auditActionEnum = pgEnum("audit_action", [
  'CREATE', 'UPDATE', 'DELETE', 'VIEW', 'EXPORT', 'IMPORT'
]);
export const onboardingStepStatusEnum = pgEnum("onboarding_step_status", [
  'PENDING', 'IN_PROGRESS', 'COMPLETED', 'SKIPPED', 'BLOCKED'
]);
export const offboardingReasonEnum = pgEnum("offboarding_reason", [
  'VOLUNTARY_RESIGNATION', 'TERMINATION_WITH_CAUSE', 'TERMINATION_WITHOUT_CAUSE',
  'CONTRACT_EXPIRED', 'RETIREMENT', 'DEATH', 'MUTUAL_AGREEMENT', 'OTHER'
]);

5. Section D: Performance Enums
export const reviewTypeEnum = pgEnum("review_type", ['SELF', 'MANAGER', 'PEER', '360']);
export const reviewStatusEnum = pgEnum("review_status", ['DRAFT', 'IN_PROGRESS', 'COMPLETED', 'SUBMITTED']);
export const goalStatusEnum = pgEnum("goal_status", ['NOT_STARTED', 'IN_PROGRESS', 'COMPLETED', 'CANCELLED']);
export const performanceCategoryEnum = pgEnum("performance_category", [
  'TECHNICAL', 'SOFT_SKILLS', 'LEADERSHIP', 'COMMUNICATION', 'PROBLEM_SOLVING', 'TEAMWORK'
]);

6. Section E: Leave Enums
export const vacationStatusEnum = pgEnum("vacation_status", [
  'PENDING', 'APPROVED', 'REJECTED', 'CANCELLED', 'COMPLETED'
]);
export const leaveTypeEnum = pgEnum("leave_type", [
  'VACATION', 'SICK_LEAVE', 'PERSONAL', 'MATERNITY', 'PATERNITY',
  'BEREAVEMENT', 'TRAINING', 'JURY_DUTY', 'OTHER'
]);
export const leaveRequestStatusEnum = pgEnum("leave_request_status", [
  'PENDING', 'APPROVED', 'REJECTED', 'CANCELLED', 'COMPLETED'
]);

7. Section F: Tables - employeeDocuments (lines ~352-395)
[Full table definition with all columns]

8. Section G: Tables - employeeProfiles (lines ~1023-1090)
[Full table definition with all columns - 40+ fields]

9. Section H: Tables - employeeContracts (lines ~1095-1153)

10. Section I: Tables - salaryHistory (lines ~1156-1181)

11. Section J: Tables - employeeAuditLogs (lines ~1184-1215)

12. Section K: Tables - employeeOnboarding, onboardingSteps (lines ~1218-1277)

13. Section L: Tables - employeeOffboarding (lines ~1280-1340)

14. Section M: Tables - employeeBenefits (lines ~1343-1377)

15. Section N: Tables - employeeTraining (lines ~1380-1422)

16. Section O: Tables - vacationAccruals (lines ~1425-1457)

17. Section P: Tables - performanceReviews, performanceReviewCriteria, performanceReviewResponses (lines ~1477-1543)

18. Section Q: Tables - performanceGoals (lines ~1546-1570)

19. Section R: Tables - vacationRequests (lines ~924-954)

20. Section S: Tables - leaveTypes, leaveRequests, leaveBalances (lines ~1573-1643)

21. Section T: Tables - employeeCommunications, communicationReadReceipts, messageTemplates (lines ~1646-1708)

Note: Due to file size, focus on extracting complete table definitions. The exact column definitions should match schema.ts exactly.

CRITICAL: Check for vacationRequests vs leaveRequests duplication:
- vacationRequests (lines ~924-954) - uses vacationStatusEnum
- leaveRequests (lines ~1594-1626) - uses leaveRequestStatusEnum
These appear to be separate tables. Include both in employees.ts for now.
</action>
<verify>
<automated>findstr /c:"export const employeeProfiles" lib\db\schema\employees.ts && findstr /c:"export const employeeContracts" lib\db\schema\employees.ts && findstr /c:"export const employeeStatusEnum" lib\db\schema\employees.ts</automated>
</verify>
<done>employees.ts created with all employee tables (25+ tables) and 20+ enums, properly organized by section</done>
</task>

<task type="auto">
<name>Task 2: Update index.ts with employees exports</name>
<files>lib/db/schema/index.ts</files>
<read_first>
- lib/db/schema/index.ts (current state)
</read_first>
<action>Update lib/db/schema/index.ts to include employees module:

1. Current state should have:
   export * from './auth';
   export * from './core';
   export * from './workflows';
   export * from './labor';

2. Add employees export at the end:
   export * from './auth';
   export * from './core';
   export * from './workflows';
   export * from './labor';
   export * from './employees';

3. Dependency order maintained:
   - auth (base)
   - core (depends on auth)
   - workflows (depends on core)
   - labor (depends on core/auth)
   - employees (depends on core/auth)

4. Note: vacationStatusEnum and leaveRequestStatusEnum may have overlapping values.
   This is acceptable - both enums can exist in employees.ts.</action>
<verify>
<automated>findstr /c:"export \* from './employees'" lib\db\schema\index.ts</automated>
</verify>
<done>index.ts updated with employees module export</done>
</task>

<task type="auto">
<name>Task 3: Verify TypeScript compilation</name>
<files>lib/db/schema/employees.ts, lib/db/schema/index.ts</files>
<action>Verify employees module compiles correctly:

1. Run TypeScript compiler:
   pnpm tsc --noEmit

2. Check for common errors in large files:
   - Duplicate identifier (if enums clash)
   - Cannot find name 'sql'
   - Type errors in FK references
   - Missing imports

3. If duplicate enum errors:
   - Check if any enums in schema.ts were already exported elsewhere
   - vacationStatusEnum might conflict if already in another module

4. Verify all 25+ tables compile

5. Check for circular dependencies (employees should not import from workflows/labor)</action>
<verify>
<automated>pnpm tsc --noEmit 2>&1 | findstr /c:"error TS" | find /c /v ""</automated>
</verify>
<done>TypeScript compilation passes with zero errors</done>
</task>

<task type="auto">
<name>Task 4: Verify no duplicate exports</name>
<files>lib/db/schema/index.ts</files>
<read_first>
- lib/db/schema.ts (original to check for duplicates)
- lib/db/schema/index.ts
</read_first>
<action>Check for duplicate exports across modules:

1. Run grep to find exported names from each module:
   grep "^export const" lib/db/schema/auth.ts
   grep "^export const" lib/db/schema/core.ts
   grep "^export const" lib/db/schema/workflows.ts
   grep "^export const" lib/db/schema/labor.ts
   grep "^export const" lib/db/schema/employees.ts

2. Check for duplicates in index.ts exports:
   - Should not have same table in two modules
   - vacationRequests vs leaveRequests: keep both (different tables)

3. Verify schema.ts re-export doesn't conflict:
   - schema.ts exports * from './schema/index'
   - This should now work since all modules are in index

4. Run final compilation check</action>
<verify>
<automated>pnpm tsc --noEmit 2>&1 | findstr /c:"error TS" | find /c /v ""</automated>
</verify>
<done>No duplicate exports detected, all modules export unique tables</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| employees → core | Import for companies/branches |
| employees → auth | Import for users |
| employees internal | Many inter-table FKs within module |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | file size | accept | Large file is acceptable for now, can split later |
| T-02-03-02 | Information Disclosure | duplicate tables | mitigate | vacationRequests and leaveRequests kept separate by design |
| T-02-03-03 | Denial of Service | enum conflict | mitigate | Check for vacationStatusEnum vs leaveRequestStatusEnum conflict |
</threat_model>

<verification>
- [ ] lib/db/schema/employees.ts created with 25+ tables
- [ ] All employee enums exported (20+ enums)
- [ ] index.ts exports employees module
- [ ] TypeScript compilation passes
- [ ] No duplicate table exports
- [ ] All cross-domain FKs compile correctly
</verification>

<success_criteria>
Measurable:
- 25+ tables migrated (employeeProfiles, employeeContracts, employeeDocuments, employeeOnboarding, onboardingSteps, employeeOffboarding, employeeBenefits, employeeTraining, vacationAccruals, performanceReviews, performanceReviewCriteria, performanceReviewResponses, performanceGoals, leaveTypes, leaveRequests, leaveBalances, employeeCommunications, communicationReadReceipts, messageTemplates, vacationRequests, salaryHistory, employeeAuditLogs)
- 20+ enums migrated (documentType, documentStatus, employeeStatus, contractType, workRegime, paymentMethod, gender, maritalStatus, bloodType, auditAction, onboardingStepStatus, offboardingReason, reviewType, reviewStatus, goalStatus, performanceCategory, vacationStatus, leaveType, leaveRequestStatus)
- TypeScript compilation: 0 errors
- No duplicate exports across modules
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-domains/02-03-SUMMARY.md`
</output>
