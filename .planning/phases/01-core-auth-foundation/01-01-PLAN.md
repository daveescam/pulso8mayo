---
phase: 01-core-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema/index.ts
  - lib/db/schema/auth.ts
  - lib/db/schema.ts
autonomous: true
requirements:
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-05
  - DOM-01
must_haves:
  truths:
    - "lib/db/schema/ directory exists with auth.ts module"
    - "Auth module exports account, users, sessions, verifications, magicLinks tables"
    - "All auth enums are exported from auth.ts"
    - "lib/db/schema/index.ts barrel exports all auth tables"
    - "Original schema.ts updated to re-export from modules"
    - "All existing imports from lib/db/schema continue to work"
  artifacts:
    - path: "lib/db/schema/auth.ts"
      provides: "Auth tables and enums (account, users, sessions, verifications, magicLinks)"
      min_lines: 80
    - path: "lib/db/schema/index.ts"
      provides: "Barrel exports aggregating all domain modules"
      exports: ["* from './auth'", "* from './core'"]
    - path: "lib/db/schema.ts"
      provides: "Updated to re-export from index.ts"
      contains: "export * from './schema/index'"
  key_links:
    - from: "lib/db/schema.ts"
      to: "lib/db/schema/index.ts"
      via: "re-export"
      pattern: "export.*from.*schema/index"
---

<objective>
Create the modular schema structure and extract auth tables into a dedicated module.

Purpose: Establish the foundation for schema modularization by creating the directory structure and migrating all auth-related tables to their own module.

Output:
- lib/db/schema/ directory with auth.ts module
- lib/db/schema/index.ts barrel file
- Updated lib/db/schema.ts for backward compatibility
</objective>

<execution_context>
@C:/Users/david/pulso29/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/david/pulso29/lib/db/schema.ts
@C:/Users/david/pulso29/AGENTS.md

Current schema.ts contains all tables in a single file (1,977 lines). Auth tables to extract:
- account (OAuth accounts)
- users (user management)
- sessions (session tracking)
- verifications (email verification)
- magicLinks (passwordless auth)

Plus auth-related enums:
- roleEnum

Drizzle ORM patterns from current file:
- Uses pgTable, text, timestamp, boolean, uuid, jsonb, integer, uniqueIndex, foreignKey, pgEnum
- Exports table definitions and types
- Exports enums with pgEnum

From AGENTS.md:
- Multi-tenant: All data scoped via tenantId
- DB schema: Single file lib/db/schema.ts (being refactored)
- Uses Drizzle ORM with Neon Postgres
</context>

<tasks>
<task type="auto">
<name>Task 1: Create schema directory structure</name>
<files>lib/db/schema/.gitkeep</files>
<action>Create the lib/db/schema/ directory if it doesn't exist. This directory will hold all domain-specific schema modules.

1. Create lib/db/schema/ directory
2. Create lib/db/schema/auth.ts (empty for now)
3. Create lib/db/schema/index.ts (empty for now)

Verification: directory exists and is writable.</action>
<verify>
<automated>dir lib\db\schema /b</automated>
</verify>
<done>lib/db/schema/ directory exists with auth.ts and index.ts files created</done>
</task>

<task type="auto">
<name>Task 2: Extract auth tables to auth.ts</name>
<files>lib/db/schema/auth.ts</files>
<read_first>
- lib/db/schema.ts (source of auth tables)
</read_first>
<action>Extract all auth-related tables and enums from lib/db/schema.ts into lib/db/schema/auth.ts:

1. Move the following exports to auth.ts:
   - roleEnum (pgEnum for roles)
   - account table (with all its columns: id, accountId, providerId, userId, accessToken, refreshToken, idToken, accessTokenExpiresAt, refreshTokenExpiresAt, scope, password, createdAt, updatedAt)
   - users table (with all columns including: id, email, emailVerified, name, role, branchId, image, timezone, authProviderId, isActive, companyId, stripeCustomerId, subscriptionStatus, subscriptionPlan, stripePriceId, subscriptionCurrentPeriodEnd, phone, createdAt, updatedAt, deletedAt)
   - sessions table (with: id, expiresAt, token, createdAt, updatedAt, ipAddress, userAgent, userId)
   - verifications table (with: id, identifier, value, expiresAt, createdAt, updatedAt)
   - magicLinks table (if present, or create structure based on better-auth patterns)

2. Include necessary imports at top:
   import { pgTable, text, timestamp, boolean, uuid, pgEnum } from "drizzle-orm/pg-core";
   import { sql } from "drizzle-orm";

3. Preserve all table definitions exactly as they are (don't change columns, types, or constraints)

4. Export all tables: export { account, users, sessions, verifications, magicLinks }

5. Also export the enum: export { roleEnum }

6. Add proper newline at end of file

Note: users table has foreign key to branches.id (added in branches table definition via foreignKey), but users table itself doesn't define the FK. Keep users table definition simple without circular references.</action>
<verify>
<automated>findstr /c:"export const account" lib\db\schema\auth.ts && findstr /c:"export const users" lib\db\schema\auth.ts && findstr /c:"export const sessions" lib\db\schema\auth.ts</automated>
</verify>
<done>auth.ts contains all auth tables with exact same definitions as original schema.ts, including proper imports and exports</done>
</task>

<task type="auto">
<name>Task 3: Create barrel exports in index.ts</name>
<files>lib/db/schema/index.ts</files>
<read_first>
- lib/db/schema/auth.ts
</read_first>
<action>Create lib/db/schema/index.ts as a barrel file that re-exports everything from domain modules:

1. Add comment header: // Schema modules barrel export

2. Export auth module: export * from './auth';

3. Add placeholder for core module (will be created in next plan): // export * from './core';

4. Ensure proper TypeScript syntax with semicolons

5. Add newline at end of file

This file serves as the single entry point for all schema modules.</action>
<verify>
<automated>findstr /c:"export \* from './auth'" lib\db\schema\index.ts</automated>
</verify>
<done>index.ts exists with auth module export</done>
</task>

<task type="auto">
<name>Task 4: Update original schema.ts for backward compatibility</name>
<files>lib/db/schema.ts</files>
<read_first>
- lib/db/schema.ts (current state)
- lib/db/schema/index.ts (new barrel)
</read_first>
<action>Update lib/db/schema.ts to maintain backward compatibility while delegating to modular structure:

1. Add deprecation comment at top:
   /**
    * @deprecated This file is being modularized. Import from lib/db/schema/index.ts instead.
    * This file is kept for backward compatibility during migration.
    */

2. Remove all table definitions (they've been moved to auth.ts)

3. Keep only the re-export:
   export * from './schema/index';

4. Alternative if full compatibility needed: keep original imports but add:
   import * as schema from './schema/index';
   export { schema };
   export * from './schema/index';

5. Preserve any top-level imports that might be used elsewhere (check for usages)

6. Ensure the file compiles and all existing exports are maintained

IMPORTANT: Do NOT delete schema.ts yet - just update it to re-export from modules. This maintains backward compatibility for existing imports throughout the codebase.</action>
<verify>
<automated>findstr /c:"export \* from './schema/index'" lib\db\schema.ts</automated>
</verify>
<done>schema.ts updated to re-export from schema/index.ts, backward compatibility maintained</done>
</task>

<task type="auto">
<name>Task 5: Verify TypeScript compilation</name>
<files>lib/db/schema.ts, lib/db/schema/index.ts, lib/db/schema/auth.ts</files>
<action>Run TypeScript compiler to verify the modular structure compiles correctly:

1. Run: pnpm tsc --noEmit

2. Check for any errors related to:
   - Missing exports
   - Circular dependencies
   - Type mismatches
   - Import path issues

3. If errors exist, fix them before marking complete

4. Verify no runtime errors by checking that all exported tables are accessible</action>
<verify>
<automated>pnpm tsc --noEmit 2>&1 | findstr /c:"error TS" | find /c /v ""</automated>
</verify>
<done>TypeScript compilation passes with zero errors</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| schema.ts → schema/index.ts | File re-export boundary - maintain backward compatibility |
| auth.ts → index.ts | Module aggregation - ensure all exports present |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | schema.ts | mitigate | Maintain re-export to prevent import breaks across codebase |
| T-01-02 | Information Disclosure | auth.ts | accept | No PII exposure - tables mirror existing structure |
</threat_model>

<verification>
- [ ] lib/db/schema/ directory exists
- [ ] lib/db/schema/auth.ts created with all auth tables
- [ ] lib/db/schema/index.ts exports auth module
- [ ] lib/db/schema.ts re-exports from index.ts
- [ ] pnpm tsc --noEmit passes
- [ ] All existing imports continue to work
</verification>

<success_criteria>
Measurable:
- 5 tables migrated (account, users, sessions, verifications, magicLinks)
- 1 enum migrated (roleEnum)
- TypeScript compilation: 0 errors
- Directory structure: lib/db/schema/ with auth.ts and index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-auth-foundation/01-01-SUMMARY.md`
</output>
